{
  "name": "tripnest-ph-functions",
  "description": "Cloud Functions for TripNest PH — sends a real FCM push notification whenever a `notifications` document is created (personal or broadcast).",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "private": true
}
